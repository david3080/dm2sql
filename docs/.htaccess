# WASM MIME type for GitHub Pages
AddType application/wasm .wasm

# Enable Cross-Origin Embedder Policy for SharedArrayBuffer (optional for better performance)
Header set Cross-Origin-Embedder-Policy "require-corp"
Header set Cross-Origin-Opener-Policy "same-origin"

# Cache WASM files
<FilesMatch "\.(wasm)$">
    ExpiresActive On
    ExpiresDefault "access plus 1 month"
</FilesMatch>